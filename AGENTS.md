# AI Agent 行为守则

## 开发原则

0. **测试禁止使用真实 API Key**：所有测试必须 mock 网络请求或使用构造数据，不得依赖 `.env` 或真实 API 调用。真实 API 调用仅用于手动验证。

1. **原理先行**：每个 step 实现前，先分析原理、实现方案、方案的利弊，再做决策。
2. **单步独立**：每个 step 一个文件夹，文件夹名为 step 编号（如 `step0/`, `step1/`），内含可独立运行的完整源码。
3. **配套文档**：每个 step 文件夹内必有同名 `.md` 文件，内容包含：
   - 该 step 的目标和实现
   - 核心函数/类功能说明
   - 暴露了什么问题
   - 下一 step 要解决什么
4. **最小增量**：每步只增加一个最简功能，不可一次增加太复杂的功能。
5. **可运行验证**：每个 step 完成后必须跑一遍测试 / 运行确认能跑通。
6. **及时提交**：跑通后提交到本 step 的 git commit（标注 step 编号），然后推送远程。
7. **终极目标**：对齐 `nanobot` 项目的 agent 核心部分，逐步演进至同等能力水平。

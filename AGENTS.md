# AI Agent 行为守则

## 目标
- 对齐 `nanobot` 项目的 agent 部分和harness部分，逐步演进至同等能力水平。
  
## 分析原则

1. **原理先行**：每个 step 实现前，要先参考`nanobot` 项目，先分析原理、选择方案、解释为什么要这样、方案的利弊，再做决策。
2. **最小增量**：每步只增加一个最简功能，不可一次增加太复杂的功能。
3. 根据最小增量原则分析本step的需求定义(proposal.md)、架构设计(design.md)、接口契约(api-spec.md)
   
## 开发原则

1. 先根据最小增量原则生成本step的需求定义(proposal.md)、架构设计(design.md)、接口契约(api-spec.md)
2. fork 必须用 UTF-8 安全方式，用 PowerShell Set-Content 做全文替换时因默认编码破坏了 UTF-8 中文
3. 严格按照规范文档实现
4. **代码注释**： 
   - 每个参数要写好明确的类型注解
   - 每个类和方法都要写好中文注释
   - 注释要写完整帮助代码理解
   - 每个方法都要写docstring
5. **单步独立**：每个 step 一个文件夹，文件夹名为 step 编号（如 `step0/`, `step1/`），内含可独立运行的完整源码。
6. **配套文档**：每个 step 实现完成后文件夹内必有同名 `.md` 配套文件，内容包含：
   - 这个step的问题背景
   - 这一step解决了什么问题以及为什么要这样做
   - 讲清楚解决问题的原理思路和具体实现
   - 该 step 的目标和实现
   - 核心函数/类功能说明
   - 暴露了什么问题
   - 下一 step 要解决什么
7.  **可运行验证**：每个 step 完成后必须跑一遍测试 / 运行确认能跑通。
8.  **代码审查**： 跑完测试后做好本step的代码审查，是否有编写规范文档？是否有按照规范实现？是否有编写配套文档？
9.  **及时提交**：跑通后提交到本 step 的 git commit（标注 step 编号，以及本阶段功能简介），然后推送远程。
10.  **测试禁止使用真实 API Key**：所有测试必须 mock 网络请求或使用构造数据，不得依赖 `.env` 或真实 API 调用。真实 API 调用仅用于手动验证。
11.  fork完记得要删除上一个step的m配套md，要记得修改